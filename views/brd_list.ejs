<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시판 목록</title>
    <style>
        .lists {
            margin: 30px; 
        }
        .lists img {
            /* 썸네일 이미지 크기 통일 */
            width: 300px;
            height: 300px;
        }
    </style>
</head>
<body>
    <% for(let i = 0; i <data.length; i++){ %>
        <div class="lists">
            <div class="num"><%- data[i].num %></div>
            <a href="/board/detail/<%- data[i].num %>">
                <!-- <img src="/upload/<%# data[i].attachfile %>" /> 첨부한 이미지 1개일 때 -->
                <img src="/upload/<%- data[i].attachfile[0] %>" />
                <!-- 여러 개 첨부할 때, 이미지가 하나만 보여야 한다면 data[i].attachfile[원하는순번값] -->
                <h2><%- data[i].title %></h2>
                <p><%- data[i].author %></p>
            </a>
        </div>
    <% } %>
    <a href="/board/insert">글쓰기</a>
</body>
</html>